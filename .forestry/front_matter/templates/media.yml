---
hide_body: true
fields:
- type: number
  name: weight
  label: "節目標題次序(例如:新城電台xxxxxx)"
  config:
    required: true
  hidden: false
  description: "由0開始 數字愈少 標題就在最底"
- type: textarea
  name: description
  label: description
  config:
    wysiwyg: false
- type: field_group_list
  name: videos
  label: videos
  fields:
  - type: text
    name: name
    label: name
    config:
      required: true
  - type: text
    name: link
    label: link
    config:
      required: true
  config:
    labelField: name
- type: datetime
  name: date
  label: date
  hidden: true
  config:
    time_format: h:mm A
